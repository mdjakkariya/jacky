{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "chat",
  "description": "Permissions for the chat drawer: let the user drag it by its header.",
  "windows": ["chat"],
  "permissions": [
    "core:window:allow-start-dragging"
  ]
}
