# Changelog — Jack orb (macOS app)

Notable changes to the **orb** desktop app. Commits scoped `(orb)`/`(ui)` land here;
CLI/engine changes live in `CHANGELOG-cli.md`.

<!-- git-cliff will prepend released sections above this line. -->
